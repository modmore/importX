######################### PLEASE READ THIS ######################################
importX does what it's supposed to do (import resources from CSV formatted data),
however the following bug(s) have been confirmed in this package:

    #3  Continuous ajax request // failing to "complete" the console.

This package has been created to allow broader and easier testing, but please do
realize this package is still an alpha.
######################### PLEASE READ THIS ######################################

importX is a utility tool for MODX Revolution to quickly import your CSV formatted values into resources.

The CSV needs to be formatted with ";" as the seperator.

The first line is the heading line, which is used to determine what column needs to be added to what resource fields.